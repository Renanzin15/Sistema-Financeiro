# === NUNCA subir: dados reais + hash da senha ===
financeiro.db
*.db
*.sqlite
*.sqlite3

# === Segredos / variáveis de ambiente ===
.env
.env.*

# === Python ===
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# === Idiomas do OCR (Tesseract) — arquivos grandes (~19 MB), baixados à parte ===
# ver estudo_ocr_conta_imagem.md / diario (Etapa 21). Não versionar: cada máquina
# baixa o por.traineddata e coloca numa pasta "tessdata" ao lado do main.py.
tessdata/
*.traineddata

# === Sistema operacional / editores ===
.DS_Store
Thumbs.db
.vscode/
.idea/

# === Backups locais grandes (opcional — descomente se não quiser versionar) ===
# backups/
