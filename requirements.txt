# Dependências do back-end (instale com: pip install -r requirements.txt)
fastapi
uvicorn[standard]
bcrypt
python-jose[cryptography]
pydantic

# --- Opcional (só necessário pro leitor de conta por imagem / PDF, o OCR) ---
# Sem isso instalado o app roda normal, só a rota /importar/conta-imagem fica indisponível.
pytesseract
Pillow
PyMuPDF
# Também exige o programa Tesseract instalado à parte (winget install -e --id UB-Mannheim.TesseractOCR)
# e, se quiser reconhecer rótulos em português, o pacote de idioma por.traineddata.
# Ver estudo_ocr_conta_imagem.md para detalhes e troubleshooting.
